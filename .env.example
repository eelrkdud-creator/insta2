# Vercel Storage > Neon integration. Server-only; never use NEXT_PUBLIC_ prefixes.
DATABASE_URL=
# Optional independent HMAC key. If omitted, DATABASE_URL is used server-side.
POST_RESULT_SIGNING_SECRET=
